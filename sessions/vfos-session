#!/usr/bin/python3
import sys
from vfos.session import launch
from vfos.common import Error
try:
    sys.exit(launch(sys.argv[1] if len(sys.argv) == 2 else ""))
except (Error, KeyboardInterrupt) as e:
    print(f"vfos-session: {e}", file=sys.stderr)
    sys.exit(1)
